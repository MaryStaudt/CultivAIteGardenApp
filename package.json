{
  "name": "cultivaite-garden",
  "version": "0.2.0",
  "private": true,
  "description": "CultivAIte AI-assisted garden planner.",
  "scripts": {
    "check": "node --check src/standalone.js && node --check api/ask.js && node --check sw.js"
  }
}
